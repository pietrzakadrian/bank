/**
 *
 * LoadingWrapper
 *
 */

import styled from 'styled-components';

import CircularProgress from '@material-ui/core/CircularProgress';

const LoadingWrapper = styled(CircularProgress)`
  margin: auto;
`;

export default LoadingWrapper;
