/**
 *
 * WidgetBeamCounter
 *
 */

import styled from 'styled-components';
import { PRIMARY_BORDER_GREY } from 'utils/colors';

const WidgetBeamCounter = styled.span`
  font-size: 12.5px;
  color: ${PRIMARY_BORDER_GREY};
`;

export default WidgetBeamCounter;
