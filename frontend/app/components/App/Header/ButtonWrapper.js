/**
 *
 * ButtonWrapper
 *
 */

import styled from 'styled-components';

const ButtonWrapper = styled.button`
  display: block;
  padding: 0;
  margin: 0 8px;

  &:hover {
    cursor: pointer;
  }
`;

export default ButtonWrapper;
