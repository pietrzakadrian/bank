/**
 *
 * P
 *
 */

import styled from 'styled-components';
import { PHONE_LANDSCAPE_VIEWPORT_WIDTH } from 'utils/rwd';

const P = styled.p`
  margin: 0 0 0 5px;
  font-size: 12.5px;
  display: flex;
  align-items: center;
  text-align: left;
`;

export default P;
