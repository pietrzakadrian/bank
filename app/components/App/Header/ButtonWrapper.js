/**
 *
 * ButtonWrapper
 *
 */

import styled from 'styled-components';

const ButtonWrapper = styled.button`
  position: relative;
  display: block;

  &:hover {
    cursor: pointer;
  }
`;

export default ButtonWrapper;
