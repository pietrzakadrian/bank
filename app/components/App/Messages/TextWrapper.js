/**
 *
 * TextWrapper
 *
 */

import styled from 'styled-components';
import { PRIMARY_LIGHT, BORDER_GREY_LIGHT } from 'utils/colors';

const TextWrapper = styled.span`
  opacity: 0.3;
`;

export default TextWrapper;
