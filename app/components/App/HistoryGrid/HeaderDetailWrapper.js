/**
 *
 * HeaderDetailWrapper
 *
 */

import styled from 'styled-components';
import { PRIMARY_BLUE_DARK } from 'utils/colors';

const HeaderDetailWrapper = styled.div`
  font-size: 16px;
  color: ${PRIMARY_BLUE_DARK};
  font-family: 'Lato';
`;

export default HeaderDetailWrapper;
